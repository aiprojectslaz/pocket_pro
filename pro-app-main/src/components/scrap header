<!-- Navbar -->

  <header class="app-header  text-white py-2">
    <!-- Logo or brand name -->
    <div class="logo">
      <router-link to="/" class="navbar-brand">
        <img src="@/assets/logos/Logo1_BlueBG_cropped.png" alt="App logo" class="img-fluid" />
      </router-link>
    </div>

    <div class="wrapper d-flex justify-content-between align-items-center">
      <!-- Navigation links -->
      <nav class="main-nav">
        <ul class="nav">
          <li class="nav-item" v-if="!authState.isLoggedIn">
            <RouterLink to="/" class="nav-link text-white">Home</RouterLink>
          </li>
          <li class="nav-item" v-if="authState.isLoggedIn">
            <RouterLink to="/procedure-list" class="nav-link text-white">Procedures</RouterLink>
          </li>
          <li class="nav-item" v-if="authState.isLoggedIn">
            <RouterLink to="/bookmarks" class="nav-link text-white">Bookmarks</RouterLink>
          </li>
          <li class="nav-item" v-if="authState.isLoggedIn">
            <RouterLink to="/quizzes" class="nav-link text-white">Quiz</RouterLink>
          </li>
          <li class="nav-item" v-if="authState.isLoggedIn" @click="logout">
            <RouterLink to="/logout" class="nav-link text-white">Logout</RouterLink>
          </li>
          <li class="nav-item" v-else>
            <RouterLink to="/login" class="nav-link text-white">Login</RouterLink>
          </li>
        </ul>
      </nav>

      <!-- Search Bar -->
      <div class="search-bar d-flex" v-if="authState.isLoggedIn">
        <input type="text" v-model="searchQuery" @keyup.enter="search" class="form-control me-2" placeholder="Search procedures..."/>
        <button @click="performSearch" class="btn btn-secondary"><fa icon="search"/></button>
      </div>

    </div> <!-- .wrapper -->
  </header>





  <div class="form-outline d-flex input-group w-auto" v-if="authState.isLoggedIn">
    <input type="search" @keyup.enter="search" class="form-control" placeholder="Search Procedures..." aria-label="Search" aria-describedby="search-addon"/>
  </div>

          <!-- Notifications -- HIDDEN NOTIFICATIONS - Fix Comments To add to site
        <div class="dropdown">
          <a class="btn text-reset me-3 dropdown-toggle d-flex align-items-center hidden-arrow" href="#" role="button" id="navbarDropdownMenuLink" data-bs-toggle="dropdown" aria-expanded="false">
            <fa icon="bell" inverse/><span class="badge rounded-pill badge-notification bg-danger">1</span>
          </a>
          <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdownMenuLink">
            <li><a class="dropdown-item" href="#">Some news</a></li>
            <li><a class="dropdown-item" href="#">Updates</a></li>
          </ul>
        </div>
        -- Notifications -->



        <!-- MAIN ROLES SECTION  -->
    <div class="main-roles" v-if="main_roles.length">
        <div v-for="(role, index) in main_roles" :key="index">
          <h3>{{ role?.attributes?.role_title }}</h3>
          <div v-for="(item, i) in role?.attributes?.description" :key="i">
            
            <!-- Check if it's a heading -->
            <div v-if="item.type === 'heading'">
              <component :is="'h' + item.level">{{ item.children[0].text }}</component>
            </div>
  
            <!-- Check if it's a paragraph -->
            <div v-if="item.type === 'paragraph'">
              {{ item.children[0].text }}
            </div>
  
            <!-- Check if it's a list -->
            <div v-else-if="item.type === 'list'">
  
              <!-- Check if it's an unordered list -->
              <ul v-if="item.format === 'unordered'">
                <li v-for="(listItem, i) in item.children" :key="i">
                  <p v-if="item.children.length > 0">{{ item.children[0].text }}</p>
                  <div v-if="listItem.type === 'list-item'">{{ listItem.children[0].text }}</div>
                  <ul v-if="listItem.type === 'list'">
                    <li v-for="(listItemX, x) in listItem.children" :key="x">
                      <div v-if="listItemX.children.length > 0">{{ listItemX.children[0].text }}</div>
                      <ul v-if="listItemX.type === 'list'">
                        <li v-for="(listItemY, y) in listItemX.children" :key="y">
                          <div v-if="listItemY.children.length > 0">{{ listItemY.children[0].text }}</div>
                        </li>
                      </ul>
                    </li>
                  </ul>
                </li>
              </ul>
  
              <!-- Check if it's an ordered list -->
              <ol v-if="item.format === 'ordered'">
                <li v-for="(listItem, i) in item.children" :key="i">
                  <p v-if="item.children.length > 0">{{ item.children[0].text }}</p>
                  <div v-if="listItem.type === 'list-item'">{{ listItem.children[0].text }}</div>
                  <ol v-if="listItem.type === 'list'">
                    <li v-for="(listItemX, x) in listItem.children" :key="x">
                      <div v-if="listItemX.children.length > 0">{{ listItemX.children[0].text }}</div>
                      <ol v-if="listItemX.type === 'list'">
                        <li v-for="(listItemY, y) in listItemX.children" :key="y">
                          <div v-if="listItemY.children.length > 0">{{ listItemY.children[0].text }}</div>
                        </li>
                      </ol>
                    </li>
                  </ol>
                </li>
              </ol>
              
            </div>
          </div>
        </div>
      </div><!-- .main-roles -->

      <!-- Definitions Section -->
      <h3>Definitions</h3>
      <ul>
        <li v-for="definition in definitions" :key="definition.id">
          <RouterLink :to="{ name: 'definition-item', params: { id: definition.id } }">
            {{ definition?.attributes?.term }}
          </RouterLink>
        </li>
      </ul>